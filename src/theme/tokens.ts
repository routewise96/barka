/**
 * src/theme/tokens.ts — единственный источник визуальных констант (раздел 6).
 *
 * Характер: яркий и игровой, но дисциплинированный.
 *  - Насыщенные высококонтрастные акценты (НЕ пастель — на дешёвых матрицах
 *    пастель сливается в грязь). Плоские заливки: без градиентов и теней
 *    (легче для 1GB RAM, быстрее рендер).
 *  - Крупные скруглённые формы, большие радиусы.
 *  - Тач-цели минимум 64dp (раздел 6, п.2), по умолчанию крупнее.
 *  - Фон спокойный тёплый-нейтральный, акценты яркие — контент не конкурирует с фоном.
 *
 * Экраны НЕ хардкодят значения — только эти токены.
 */

/** Минимальная тач-цель — 64dp (раздел 6, п.2). Базовый минимум для всех кнопок. */
export const TOUCH_TARGET_MIN = 64;

export const colors = {
  /** Спокойный тёплый-нейтральный фон — не конкурирует с картинкой контента. */
  background: '#FFF3DE',
  /** Поверхность карточек/кнопок поверх фона. */
  surface: '#FFFFFF',

  // — насыщенные тёплые акценты (плоские заливки) —
  /** Основной тёплый «земляной» акцент (терракота). */
  primary: '#E14B26',
  /** Солнечный жёлтый акцент. */
  accent: '#FFB300',
  /** Контрастный «прохладный» акцент для разнообразия (бирюза). */
  accentCool: '#0E8FA8',

  // — обратная связь выбора (choices) —
  /** Правильный выбор — насыщенный зелёный. */
  correct: '#2B9348',
  /** Неправильный выбор — насыщенный красный. */
  wrong: '#E01E37',

  /** Текст/иконка поверх насыщенной заливки. */
  onAccent: '#FFFFFF',
  /** Тёмный тёплый «чернильный» — иконки на светлом, текст режима учителя. */
  ink: '#26211C',
  /** Текст только для режима учителя (НЕ для ребёнка). */
  textTeacher: '#26211C',
  /** Полупрозрачное затемнение для press-state. */
  pressOverlay: 'rgba(0, 0, 0, 0.10)',
} as const;

/** Крупные отступы — интерфейс для маленьких детей. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 40,
  xxl: 64,
} as const;

/** Большие радиусы скругления (игровые округлые формы). */
export const radius = {
  sm: 12,
  md: 20,
  lg: 32,
  /** Полностью круглая форма (кнопки-«таблетки», круглые цели). */
  pill: 999,
} as const;

/** Крупные размеры — интерфейс для маленьких детей, тач-цели ≥ 64dp. */
export const sizes = {
  /** Базовая крупная кнопка (заметно больше минимума 64dp). */
  bigButton: 112,
  /** Плитка варианта выбора в ChoiceRow. */
  choice: 128,
  /** Обложка в списках. */
  cover: 168,
  /** Кнопка «назад» — крупная, всегда в одном месте (раздел 6, п.6). */
  back: 72,
  /** @deprecated старый ключ каркаса — оставлен для совместимости, = back. */
  backArrow: 72,
  /** Размер глифа стрелки внутри кнопки «назад». */
  backGlyph: 38,
} as const;

/** Толщина рамки визуальной обратной связи выбора. */
export const borders = {
  feedback: 6,
} as const;

/** Лёгкие, дешёвые для слабого GPU параметры реакции на нажатие. */
export const motion = {
  /** Масштаб при нажатии (мгновенная визуальная реакция, без тяжёлых анимаций). */
  pressScale: 0.96,
} as const;
