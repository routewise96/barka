/**
 * src/sharing/shareApk.ts — ОФЛАЙН-шеринг самого APK-ФАЙЛА (отложено на будущее).
 *
 * Отдаёт установленный APK через Android-интент (ACTION_SEND), передача идёт без
 * сети (Bluetooth/Nearby/файл). Это тяжёлый путь и пока ЗАГЛУШКА — на этапе MVP
 * используется упрощённый вариант: шеринг текстовой ссылки на скачивание,
 * см. src/sharing/shareAppLink.ts. Этот файл оставлен как точка расширения,
 * чтобы офлайн-передача файла появилась позже без переделки архитектуры.
 */

/** Поделиться собственным APK приложения через системный интент. */
export async function shareOwnApk(): Promise<void> {
  throw new Error('sharing/shareApk.shareOwnApk: не реализовано (каркас)');
}
