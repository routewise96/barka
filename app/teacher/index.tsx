/**
 * app/teacher/index.tsx — режим учителя (Фаза 2), скрыт за жестом/пином.
 * ЗАГЛУШКА каркаса. Единственное место с текстом для взрослых (раздел 6, п.5).
 */
import { Text, View } from 'react-native';

export default function Teacher() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Режим учителя (Фаза 2) — заглушка</Text>
    </View>
  );
}
